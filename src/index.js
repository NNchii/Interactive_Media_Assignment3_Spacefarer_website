import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Correct path for index.css
import App from './App'; // Correct path for App component

ReactDOM.render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>,
  document.getElementById('root')
);
